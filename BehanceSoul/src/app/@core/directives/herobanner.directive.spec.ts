import { HerobannerTextDirective } from './herobanner-text.directive';

describe('HerobannerTextDirective', () => {
  it('should create an instance', () => {
    const directive = new HerobannerTextDirective();
    expect(directive).toBeTruthy();
  });
});
